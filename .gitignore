# Prerequisites
*.d

# Compiled Object files
*.slo
*.lo
*.o
*.obj

# Precompiled Headers
*.gch
*.pch

# Linker files
*.ilk

# Debugger Files
*.pdb

# Compiled Dynamic libraries
*.so
*.dylib
*.dll

# Fortran module files
*.mod
*.smod

# Compiled Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app

# debug information files
*.dwo

# CMake build directory
build/

CLAUDE.md

# Claude Code local state (worktrees, settings, session data)
.claude/

# Internal-facing, not published
docs/
.vscode/
.superpowers/

# mypy writes its cache into the working directory, i.e. the repo root. It
# self-protects with a nested ignore it generates, but relying on that means
# the protection disappears if the directory is ever recreated by hand.
.mypy_cache/

# scripts/ holds the one Python file in this repo. Running it directly leaves
# no bytecode, but IMPORTING it (which is how its helpers get unit-probed) does
# -- and a stray `git add -A` at that moment commits it. Listed for the same
# reason scratch_mypy/ is: the window is narrow and the mistake is silent.
__pycache__/

# The scratch directory used to run mypy on probe programs OUTSIDE the corpus.
# Empty in a clean tree, so `git status` never mentions it -- which is exactly
# why it needs listing here rather than being noticed in time: one `git add
# -A` while it holds probes is all it takes to commit them.
scratch_mypy/
